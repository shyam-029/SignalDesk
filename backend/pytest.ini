[pytest]
# Run async tests without marking each one (pytest-asyncio auto mode).
asyncio_mode = auto
# Make the `app` package importable when running pytest from backend/.
pythonpath = .
# Test discovery root.
testpaths = tests
